<% if @item.destroyed? %>
  $('#item-' +<%= @item.id %>).hide();
 <% else %>
  $('#item-' +<%= @item.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] = 'Item could not be' %></div>");
 <% end %>